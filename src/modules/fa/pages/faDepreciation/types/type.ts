export interface FaDepreciationRun {
  id: number;
  stateId: number;
  stateName: string;
  documentNumber: number;
  documentDate: string;
  comment: string;
  createdDate: string;
}
export interface FaDepreciationRecord {
  id: number;
  stateId: number;
  stateName: string;
  documentNumber: number;
  documentDate: string;
  comment: string;
  createdDate: string;
}
